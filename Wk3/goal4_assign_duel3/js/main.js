/*Name: Albert MartinezDate: 10/18/2014Assignment: Goal4: Duel #3 *///self-executing function(	function(){	    // verify if document is setup properly using console.log		// console.log("Fight!");	    	    var players = [{p1: {							name: "Modern Man", 							damage: 35, 							health: 100}						},				    	{p2: {					   		name: "Retro Rival", 					   		damage: 35, 					   		health: 100}					   	}		];		var dom = {			p1Stats: document.getElementById('modern'),			p2Stats: document.getElementById('retro'),			round: document.getElementById('round'),			scores: document.getElementById('scores'),			fBtn: document.getElementById('fBtn')		};				var roundNum = 0;		function fight(){			console.log("Top of the fight function");					// given players max damage, calculate the min damage (1/2 the players max Damage)			var minDam1 = ((players[0].p1.health) * .5);			var minDam2 = ((players[1].p2.health) * .5);			console.log("Calculating minDam1 & 2");					// calculate a different random # for each player that falls between their min and max damage			var f1 = Math.floor(Math.random() * (players[0].p1.damage - minDam1) + minDam1);			var f2 = Math.floor(Math.random() * (players[1].p2.damage - minDam2) + minDam2);			console.log("Create random #s");					// store the difference of the random # from the players health into the players health index[2]			players[0].p1.health -=f1;			players[1].p2.health -=f2;			console.log("Clash! ((B(OO)M))! Pow! ");				dom.p1Stats.innerHTML = players[0].p1.name + ': ' + players[0].p1.health;				dom.p2Stats.innerHTML = players[1].p2.name + ': ' + players[1].p2.health;					// create a variable named results and assign it the outcome of the winnerCheck function			var results = winnerCheck();					// write to the console the value of the results variable			console.log(results);						// check if there is a next round or stop to announce the winner			if (results === "no winner") {				roundNum++; //				dom.round.innerHTML = 'Round ' + roundNum + ' complete!';			} else {				dom.fBtn.innerHTML = "END!"				dom.scores.setAttribute('class', 'heading clear');				// otherwise display an alert with the winner, break the loop, and exit program					dom.scores.innerHTML = results;				exit();			}		};				// target the button element and set it to the variable aFigh		var fBtn = document.getElementById('fBtn');		// assign aFight an .onclick event. when clicked, fight() is triggered		fBtn.onclick = function(){fight();};		console.log("------ WINCHECK() ------");		// create a function called winnerCheck to check if a player has won at the end of every round		function winnerCheck(){			// create a "no winner" outcome and set it to a variable named result			var result = "no winner";			// use a conditional statement to check if both players' health fell below 0			if (players[0].p1.health < 1 && players[1].p2.health < 1) {				// if above is true, both players lose				result = "You both lost";				// since they both didn't lost then check if one of the players' health is below 0 to determine the loser and the other the winner			} else if (players[0].p1.health < 1) {				result = players[1].p2.name + " Wins!";			} else if (players[1].p2.health < 1) {				result = players[0].p1.name + " Wins!";			}			// return the value to the result variable to make this function reuseable			return result;		};		})();